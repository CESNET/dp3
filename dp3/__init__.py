from types import SimpleNamespace

# An object to store program-level globals, like loaded configuration or
# instances of main components.
# Its contents are set up at run-time during initialization
g = SimpleNamespace()
